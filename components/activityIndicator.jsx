import { IconLoader } from "@tabler/icons";
import styles from "../styles/ActivityIndicator.component.module.less";

const ActivityIndicator = () => {
  return <IconLoader className={styles.indicator} />;
};

export default ActivityIndicator;
